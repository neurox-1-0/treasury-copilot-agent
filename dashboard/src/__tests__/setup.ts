/**
 * dashboard/src/__tests__/setup.ts
 * =================================
 *
 * Global test setup file.  Imported by Vitest before every test file via
 * ``vite.config.ts → test.setupFiles``.
 *
 * What this does
 * --------------
 * - Imports ``@testing-library/jest-dom`` to extend Vitest's ``expect`` with
 *   DOM matchers like ``toBeInTheDocument()``, ``toHaveTextContent()``, etc.
 */
import "@testing-library/jest-dom";
